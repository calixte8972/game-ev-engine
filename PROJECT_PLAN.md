# 真人桌面游戏概率与 EV 计算引擎：项目计划

> 项目暂名：`game-ev-engine`
> 首个实现游戏：标准百家乐（Punto Banco）
> 实现语言：Rust
> 当前阶段：Phase 5 进行中
> 文档用途：这是后续开发的主路线图。每完成一个阶段，就在本文更新状态、决定和下一步。

## 0. 已确认的 v0.1 规则基线

规则来源：[`docs/rules/baccarat_rules_summary.md`](docs/rules/baccarat_rules_summary.md)。实现、测试常量和用户界面中的规则说明均以该文档为准；规则发生变化时，先更新规则文档，再修改代码。

- 标准游戏：Punto Banco；
- 牌靴：默认 8 副牌，共 416 张；
- 标准庄：赢时净赔付 `0.95:1`（5% 佣金）；
- 免佣庄作为可配置变体：一般庄赢净赔付 `1:1`，庄以 6 点获胜时净赔付 `0.5:1`；
- 闲：赢时净赔付 `1:1`；
- 和：赢时净赔付 `8:1`；
- 和局时庄注、闲注均 Push，退回本金；
- 第一批边注：庄对（Banker Pair）和闲对（Player Pair）；
- 庄对、闲对均只判断对应一方起手两张牌的 Rank 是否相同，不要求花色相同；
- 庄对、闲对赢时净赔付均为 `11:1`；
- 用户交互语言：中文；命令帮助、结果表和错误信息均使用中文；
- 终端命令名、选项和牌面输入使用标准 ASCII 形式，例如 `AS`、`10H`；中文只用于 CLI 的帮助、结果和错误说明，不进入核心牌模型；
- v0.1 不推断不可见烧牌的牌面，只从牌靴扣除用户明确录入的已知牌。

规则文档中幸运 7 类玩法的可下注局数存在一处待核对差异，但它不影响 v0.1 的主注、庄对和闲对实现，详见“风险清单”。

## 1. 项目目标

独立构建一个本地运行、结果可复现、规则可配置的概率与期望收益（EV）计算引擎。

第一阶段专注百家乐：根据一靴牌当前剩余的真实牌组成，精确计算下一把各结果及下注项的概率和 EV，包括：

- 闲赢（Player）概率与 EV；
- 庄赢（Banker）概率与 EV；
- 和局（Tie）概率与 EV；
- 后续逐步加入对子、大小、庄/闲特殊牌型等边注；
- 每把结束并录入已出现的牌后，更新牌靴状态并重新计算下一把；
- 明确区分“理论初始牌靴概率”和“给定当前剩余牌后的条件概率”。

项目也作为 Rust 学习工程，代码优先考虑：正确、清晰、可测试，然后再做性能优化。

## 2. 边界与原则

### 2.1 第一版包含

- 标准 Punto Banco 发牌和补牌规则；
- 1～8 副牌的可配置牌靴（实现不硬编码上限）；
- 录入已经移除或已经出现的牌；
- 精确枚举下一把所有可能发牌路径；
- 计算各下注项概率、赔率、EV、庄家优势；
- 命令行界面（CLI）；
- 单元测试、基准测试和结果交叉验证；
- 可保存及恢复当前牌靴状态。

### 2.2 第一版不包含

- 自动识别视频画面中的牌；
- 自动下注、操作博彩网站或绕过平台限制；
- 多靴资金管理、预测输赢序列或所谓“路单规律”；
- 一开始就开发 GUI、Web 服务或手机 App；
- 用浮点近似冒充精确概率。

### 2.3 设计原则

1. **规则与计算分离**：游戏规则、赔率规则、牌靴状态和概率算法相互独立。
2. **核心计算无副作用**：同样的牌靴和规则必须得到同样的结果。
3. **先精确、后加速**：先获得可信基准，再引入缓存、聚合和并行计算。
4. **全牌建模、按需聚合**：底层保留点数、牌面和花色，以支持未来边注；主注计算时可按百家乐点数聚合以提速。
5. **所有结果可验证**：每个概率总和、路径权重和 EV 都应具备自动化检查。
6. **不把策略宣传成获利保证**：EV 是给定规则和已知信息下的数学期望，不保证单次或短期结果。

## 3. 必须先定义清楚的数学模型

### 3.1 牌靴状态

完整状态至少包括：

- 使用的副牌数；
- 每个牌面与花色当前剩余张数；
- 已移除的牌（烧牌、已发牌、其他可确认不可用的牌）；
- 未知但已移除的牌数（如现场存在不可见烧牌时）；
- 当前采用的游戏规则和赔付表。

基础牌模型建议使用 13 个牌面 × 4 个花色的计数，而不是只记录 0～9 点：

- A 计 1 点；
- 2～9 按牌面计点；
- 10、J、Q、K 均计 0 点；
- 对子类边注需要区分牌面；
- Perfect Pair 等边注还需要区分花色。

主注算法可把剩余牌临时聚合成 0～9 点的计数，以减少分支数量。

### 3.2 下一张牌的条件概率

若当前总剩余牌数为 `N`，某类牌剩余 `c` 张，则下一张属于该类的概率为：

```text
P(next = card_class) = c / N
```

抽出后立刻减少对应计数。因此这是无放回抽样，不能把每张牌当成互相独立。

一条完整发牌路径的概率，是路径上每次条件概率的乘积。相同最终结果的所有合法路径概率相加，得到该结果概率。

### 3.3 标准百家乐流程

发牌顺序：

```text
闲第 1 张 → 庄第 1 张 → 闲第 2 张 → 庄第 2 张
```

随后按规则判断：

1. 任一方前两张为自然 8 或自然 9 时，双方均停止补牌；
2. 否则闲先按点数决定是否补第三张；
3. 庄是否补第三张，取决于庄两张点数，以及闲是否补牌和闲第三张的点数；
4. 双方最终点数对 10 取模后比较，得到庄、闲或和。

补牌规则要实现为独立、可直接用表格测试的纯函数，不能散落在枚举代码中。

### 3.4 概率与 EV

对于每下注 1 单位，使用“净盈利”定义 EV：

```text
EV = Σ P(outcome_i) × net_profit(outcome_i)
```

例如标准主注在和局时退回本金（净盈利为 0）：

```text
Player EV = P(Player) × player_win_payout - P(Banker)
Banker EV = P(Banker) × banker_win_payout - P(Player)
Tie EV    = P(Tie) × tie_win_payout - (P(Player) + P(Banker))
```

典型规则下：

- 闲赢净赔付通常为 `1:1`；
- 庄赢如收 5% 佣金，净赔付为 `0.95:1`；
- 和局赔率可能为 `8:1` 或 `9:1`，必须由配置决定；
- 不同赌场的免佣庄、庄 6 半赔、边注赔付表均作为不同规则变体处理。

同时输出：

```text
house_edge = -EV
return_to_player = 1 + EV
```

这里的 `return_to_player` 按每下注 1 单位并包含返还本金的口径表达。界面必须标注口径，避免把净收益与总返还混淆。

### 3.5 数值表示

分两层处理：

- **计算层**：优先保留整数路径权重或有理数，避免累计浮点误差；
- **展示层**：转换为 `f64` 或十进制定点格式，显示概率、百分比和 EV。

第一版可以先用经过严格误差检查的 `f64` 完成算法原型，同时保留一套小规模精确测试；在确定性能和依赖取舍后，再决定是否全面使用 `num-rational` / `num-bigint`，或使用“共同分母 + 大整数路径计数”。

无论采用哪种表示，都必须验证：

```text
P(Banker) + P(Player) + P(Tie) ≈ 1
```

## 4. 规则差异与配置项

第一版先明确支持一个基准规则集，所有变化通过配置表达：

```text
GameRules
├── decks
├── drawing_rules
├── banker_commission
├── banker_special_payouts
├── tie_payout
├── side_bet_paytables
├── burn_card_policy
└── cut_card / penetration policy
```

需要特别区分：

- 赌场公布的赔付是“净赔付”还是“含本金返还”；
- 庄注佣金是在每手结算还是累计结算；
- 和局时庄/闲主注是否 Push；
- 是否采用免佣百家乐以及庄 6 的特殊赔付；
- 边注判定使用前两张牌，还是包括第三张牌；
- 边注是否依赖牌面、花色、双方组合或补牌过程；
- 现场不可见烧牌应被当成随机未知信息，而不是凭空从某一牌面扣除。

## 5. 推荐的 Rust 工程结构

初期保持单个 crate；只有在确有需要时再拆 workspace。

```text
game-ev-engine/
├── Cargo.toml
├── README.md
├── PROJECT_PLAN.md
├── src/
│   ├── main.rs              # CLI 入口
│   ├── lib.rs               # 对外暴露稳定 API
│   ├── card.rs              # Suit、Rank、Card、百家乐点数
│   ├── shoe.rs              # 剩余牌计数、移除/恢复、状态校验
│   ├── baccarat/
│   │   ├── mod.rs
│   │   ├── hand.rs          # 手牌与点数
│   │   ├── rules.rs         # 自然牌和补牌规则
│   │   ├── outcome.rs       # Banker / Player / Tie 及细分结果
│   │   ├── enumerate.rs     # 精确发牌路径枚举
│   │   └── bets.rs          # 主注与边注结算
│   ├── probability.rs       # 概率/权重表示与归一化
│   ├── ev.rs                # 通用 EV 汇总
│   ├── config.rs            # 规则和赔付配置
│   ├── state.rs             # 保存/恢复 session
│   └── cli.rs               # 命令和展示
├── tests/
│   ├── baccarat_rules.rs
│   ├── initial_shoe.rs
│   ├── depleted_shoe.rs
│   ├── ev_paytables.rs
│   └── fixtures/
└── benches/
    └── next_hand.rs
```

可能使用的依赖在进入实现阶段后再逐项决定，避免一开始堆依赖。候选包括：

- `clap`：命令行参数与子命令；
- `serde`、`serde_json`：配置和牌靴状态持久化；
- `thiserror`：结构化错误；
- `num-bigint`、`num-rational`：精确数值；
- `rayon`：确认存在收益后再用于顶层分支并行；
- `criterion`：性能基准；
- `proptest`：牌靴和概率不变量测试。

## 6. 核心类型草案

以下只是方向，不在规划阶段锁死 API：

```rust
pub enum Suit { Clubs, Diamonds, Hearts, Spades }

pub enum Rank {
    Ace, Two, Three, Four, Five, Six, Seven,
    Eight, Nine, Ten, Jack, Queen, King,
}

pub struct Card {
    pub rank: Rank,
    pub suit: Suit,
}

pub struct Shoe {
    // 13 × 4，每格为该牌当前剩余张数
    counts: [[u16; 4]; 13],
    total: u16,
}

pub enum MainOutcome {
    Player,
    Banker,
    Tie,
}

pub struct OutcomeProbability<T> {
    pub outcome: T,
    pub probability: Probability,
}

pub trait Bet {
    type Outcome;

    fn settle(&self, outcome: &Self::Outcome) -> NetPayout;
}
```

边注扩展时，很可能需要比 `MainOutcome` 更丰富的 `RoundResult`：初始两张牌、第三张牌、最终点数、是否对子、牌面和花色等。枚举器应产出足够完整的回合结果，再由各下注项独立结算，避免为每个边注重复跑一次全量枚举。

## 7. 计算算法路线

### 7.1 正确性原型

递归枚举所有合法的点数抽牌路径：

1. 从当前计数中选择下一张牌类别；
2. 乘上该分支的条件概率或路径权重；
3. 从局部牌靴状态扣除该牌；
4. 按固定顺序发前四张牌；
5. 调用纯函数判断闲是否补牌；
6. 根据闲第三张调用纯函数判断庄是否补牌；
7. 生成完整 `RoundResult`；
8. 汇总各结果和下注项的权重；
9. 回溯牌靴状态。

一局最多发 6 张牌。主注只按 0～9 点聚合时，分支规模是可控的；依赖具体牌面或花色的边注按需要切换到更细的牌类别。

### 7.2 性能优化顺序

只有基准测试证明需要时，依次考虑：

1. 原地扣牌和回溯，减少克隆；
2. 主注使用 10 类百家乐点数计数；
3. 对等价状态做 memoization；
4. 一次枚举同时结算多个下注项；
5. 预计算补牌规则和最终点数；
6. 仅在顶层分支使用并行；
7. 为实时录牌场景研究增量更新，而不是每次完全重算。

不能为了速度破坏以下能力：结果复现、规则测试、精确性校验和边注扩展。

### 7.3 未知移除牌的处理

现场常见“知道有若干张牌被烧掉，但不知道是什么”。这与“这些牌仍在牌靴里”不是同一个状态。

可分阶段支持：

- MVP：只接受已知牌面的移除；未知烧牌在文档中明确限制；
- 后续：对所有可能的未知移除组合进行边缘化，计算给定可见信息下的条件概率；
- 若未知数量太大导致精确计算昂贵，可提供带置信区间的 Monte Carlo 模式，并明确标注为估算结果。

## 8. 边注扩展策略

边注不要一次全部实现。每个边注必须先收集并确认赌场的正式规则和赔付表，再编码。

推荐顺序：

1. Player Pair / Banker Pair；
2. Either Pair / Any Pair；
3. Perfect Pair；
4. Big / Small；
5. Player/Banker Bonus 或 Dragon Bonus；
6. Lucky 6 / Super 6；
7. Dragon 7 / Panda 8 等依赖最终张数和点数的边注；
8. 其他现场特定边注。

每个边注都要新增：

- 规则说明与判定时点；
- 可配置赔付表；
- `RoundResult → NetPayout` 的结算函数；
- 正例、反例和边界测试；
- 初始牌靴概率的独立验证数据；
- 当前牌靴下概率和 EV 输出。

## 9. 验证方案

概率引擎最危险的问题不是编译失败，而是“运行正常但数字悄悄算错”。验证至少分五层。

### 9.1 规则表测试

- 自然 8/9 不补牌；
- 闲 0～5 补牌、6～7 停牌；
- 庄在每个两张点数及闲第三张点数下的全部组合；
- 点数对 10 取模；
- 所有庄、闲、和比较结果。

### 9.2 牌靴不变量

- 初始化后总张数等于 `52 × decks`；
- 每次扣牌总数恰好减少 1；
- 不能扣除不存在的牌；
- 回溯后状态与进入分支前完全相同；
- 所有计数永不为负；
- 保存再加载后状态完全一致。

### 9.3 概率不变量

- 所有分支概率非负；
- 互斥结果概率和为 1（允许严格定义的小误差）；
- 对称构造的小牌靴得到预期对称结果；
- 同一牌靴重复计算结果一致；
- 改变花色但保持点数/牌面等价时，主注结果不变。

### 9.4 基准结果

使用标准完整牌靴的公开组合分析结果作为回归基准。常见 8 副牌标准规则下，粗略参考值约为：

```text
Banker ≈ 45.86%
Player ≈ 44.62%
Tie    ≈  9.52%
```

这些仅用于规划阶段的量级检查，不作为最终测试常量。实现测试前应找到可靠来源或使用第二套独立算法生成精确常量，并记录规则、牌数、舍入位数和来源。

### 9.5 双算法交叉验证

- 小型自定义牌靴：手工枚举或穷举所有具体牌排列；
- 生产算法：按等价牌类别聚合枚举；
- 两种方法在小牌靴上的每项结果必须一致；
- Monte Carlo 仅作为第三种 sanity check，不能代替精确算法测试。

## 10. CLI 用户流程草案

```text
# 新建 8 副牌标准百家乐 session
game-ev new --game baccarat --decks 8 --rules standard

# 录入已经出现的牌
game-ev remove AS 9H KD 3C

# 查看当前牌靴摘要
game-ev shoe

# 计算下一把主注概率和 EV
game-ev analyze --bets player,banker,tie

# 使用特定赔付配置
game-ev analyze --config rules/casino-a.toml

# 撤销误录的一张牌
game-ev undo

# 保存/载入
game-ev save session.json
game-ev load session.json
```

输出示例口径：

```text
Remaining cards: 412 / 416
Rules: 8 decks, Banker commission 5%, Tie pays 8:1

Bet       Probability    Net payout    EV / unit    House edge
Player      ...             1.00          ...          ...
Banker      ...             0.95          ...          ...
Tie         ...             8.00          ...          ...
```

必须同时显示剩余牌数和规则摘要，防止用户拿错 session 或误解赔付条件。

## 11. 分阶段实施路线图

### Phase 0：需求冻结与规则样本

状态：`已完成（主注与第一批边注）`

目标：确定第一版唯一的基准规则，避免边实现边改变数学口径。

任务：

- [x] 确认首个目标是标准 Punto Banco；
- [x] 确认默认使用 8 副牌（底层仍使用可配置的副牌数模型）；
- [x] 确认标准庄净赔付 `0.95:1`、和局净赔付 `8:1`，和局时庄/闲主注 Push；
- [x] MVP 不处理不可见烧牌的牌面，只扣除明确录入的已知牌；
- [x] 第一批边注确定为庄对和闲对，净赔付均为 `11:1`；
- [x] 确定 CLI 说明和输出语言为中文，牌面输入使用 `AS`、`10H` 等 ASCII 记法；
- [x] 已确认规则写入 `docs/rules/baccarat_rules_summary.md`。

完成标准：我们能用一页规则文档无歧义地人工判定任意一手牌。

### Phase 1：Rust 工程与牌靴模型

状态：`已完成`

Rust 学习重点：Cargo、模块、枚举、结构体、所有权、借用、`Result`、单元测试。

任务：

- [x] `cargo init`，建立 library + binary；
- [x] 实现 `Suit`、`Rank`、`Card` 及 ASCII 字符串解析；
- [x] 实现百家乐点数转换；
- [x] 使用扁平 `[u8; 52]` 计数实现多副牌 `Shoe`；
- [x] 实现安全的扣牌、恢复、原子批量录入和状态校验；
- [x] 为当前全部核心类型补齐单元测试；
- [x] 使用 `cargo fmt`，并以 `cargo clippy --all-targets --all-features -- -D warnings` 严格检查。

完成标准：能建立 8 副牌牌靴，正确录入/撤销牌，并通过所有状态不变量测试。

### Phase 2：百家乐规则内核

状态：`已完成`

Rust 学习重点：模式匹配、纯函数、迭代器、测试表驱动设计。

任务：

- [x] 实现两张/三张手牌点数；
- [x] 实现 Natural 判定；
- [x] 实现闲补牌规则；
- [x] 完整实现庄补牌表；
- [x] 实现发牌流程状态机；
- [x] 实现 `RoundResult`；
- [x] 对规则表做穷尽测试。

完成标准：给定一组具体牌，能无随机性地得到正确的发牌过程和最终结果。

### Phase 3：下一把精确概率引擎

状态：`已完成`

Rust 学习重点：递归、可变借用、回溯、泛型数值、错误边界。

任务：

- [x] 建立六张共同分母与庄、闲、和精确整数权重容器；
- [x] 先实现慢但直观的具体牌穷举器，用于小牌靴测试；
- [x] 实现按百家乐点数聚合的主注枚举器；
- [x] 汇总庄、闲、和概率；
- [x] 验证概率和及牌靴回溯不变量；
- [x] 与完整初始牌靴基准结果比对；
- [x] 记录一次计算的耗时和内存基线。

完成标准：对任意合法已知牌靴，稳定输出下一把精确的庄、闲、和概率，并通过双算法交叉验证。

Phase 3 基准记录（2026-08-27）：

- 完整 8 副牌的精确权重为：Player `2,230,518,282,592,256`、Banker
  `2,292,252,566,437,888`、Tie `475,627,426,473,216`，总权重
  `4,998,398,275,503,360`；结果与 [Wizard of Odds 的 8 副牌分析表](https://wizardofodds.com/games/baccarat/basics/)
  中的组合数和概率一致；
- release 构建使用 `benches/main_outcomes.rs`，执行 100 次完整牌靴计算，
  总耗时 `417.109 ms`，单次平均 `4.171 ms`；单次运行观测到的进程峰值工作集约
  `4,558,848 bytes`（约 `4.35 MiB`）。该数据是当前开发主机上的基线，不代表所有设备的性能保证。

### Phase 4：赔付和 EV 层

状态：`已完成`

Rust 学习重点：trait、配置反序列化、领域类型设计。

任务：

- [x] 定义统一的净赔付口径；
- [x] 实现 Player、Banker、Tie 结算；
- [x] 支持 5% 庄佣金；
- [x] 支持免佣庄变体：庄赢通常赔 `1:1`，庄 6 点获胜赔 `0.5:1`；
- [x] 支持可配置和局赔率；
- [x] 输出 EV、House Edge、RTP；
- [x] 对赔付和 EV 公式做独立测试；
- [x] 防止把赔率 `8:1` 误写成总返还倍数 8。

完成标准：给定概率和赔付表，能准确计算每个主注每下注 1 单位的 EV。

### Phase 5：可用 CLI 与 session

状态：`进行中`

Rust 学习重点：CLI 设计、Serde、文件 I/O、错误信息。

任务：

- [ ] 实现 `new`、`remove`、`undo`、`shoe`、`analyze`；
- [ ] 支持 JSON/TOML 规则配置；
- [ ] 支持 session 保存和恢复；
- [ ] 输出清晰的概率/EV 表；
- [ ] 对错误牌、重复扣牌、损坏 session 给出明确提示；
- [ ] 编写 README 快速开始。

完成标准：不修改代码，仅用命令行即可连续录入一靴牌并逐手计算下一把。

### Phase 6：第一批边注

状态：`进行中`

Rust 学习重点：trait 扩展、组合结果建模、共享枚举结果。

任务：

- [x] 实现闲对（Player Pair）和庄对（Banker Pair）；
- [x] 按起手两张牌 Rank 相同判定，不比较花色；
- [x] 按各自净赔付 `11:1` 计算概率和 EV；
- [x] 增加任意对子 `5:1`，按庄对或闲对的并集计算而不是直接相加；
- [x] 对子使用 13 类 Rank 计数，幸运 7 系列复用点数枚举；
- [x] 实现幸运 7 `6/15:1` 与超级幸运 7 `30/40/100:1` 的分档概率和 EV；
- [ ] 再实现 Perfect Pair，并扩展到花色；
- [ ] 实现 Big / Small；
- [ ] 每项边注独立配置赔率；
- [ ] 建立边注规则夹具和回归常量。

完成标准：新增边注不需要修改百家乐主流程，只需新增判定/结算与必要的枚举粒度。

### Phase 7：性能、可观测性与发布

状态：`待开始`

Rust 学习重点：profiling、benchmark、并行安全、发布构建。

任务：

- [ ] 用 Criterion 建立各阶段基准；
- [ ] 找出真正热点后再优化；
- [ ] 比较克隆、原地回溯、缓存和并行方案；
- [ ] 为结果增加算法版本和规则指纹；
- [ ] 构建 Windows release binary；
- [ ] 建立 CI：fmt、clippy、test、bench smoke test；
- [ ] 固定 v0.1.0 的输入输出格式。

完成标准：常见电脑上能满足逐手录入后的交互式等待时间，并可重复构建发布版本。

### Phase 8：WebAssembly 图形界面与其他游戏

状态：`进行中`

- [x] 本地 Web UI 与浏览器 WebAssembly 计算；
- [x] 导入百家乐整靴历史记录并回放；
- [x] 增加二十一点有限牌靴起手动作分析；
- [x] 支持 S17/H17、3:2/6:5、晚投降、DAS 和美式 Peek 配置；
- [x] 输出 Stand、Hit、Double、Split、Surrender 与 Insurance EV；
- [x] 对美式未知暗牌使用 Peek 条件和后验占牌模型；
- [ ] 将 Split 升级为多手联合耗牌与再次分牌的精确递归；
- [ ] 建立下注前（pre-deal）总体优势，随后才能严谨接入凯利初始下注金额；
- [ ] 取得真实 21 点 CSV 字段和动作编码后实现历史回放；
- [ ] 图表化展示牌靴变化与每手 EV；
- [ ] 扩展到龙虎等游戏；
- [ ] Monte Carlo 模式和未知烧牌边缘化。

## 12. 每一步的协作方式

后续每次只推进一个可验证的小目标：

1. 先说明本步要学的 Rust 概念和要解决的数学问题；
2. 我们共同确认少量类型/API；
3. 编写最小实现和测试；
4. 运行 `cargo fmt`、`cargo clippy`、`cargo test`；
5. 查看实际输出并解释代码；
6. 在本文勾选任务，记录重要决定；
7. 再进入下一步。

建议单次开发粒度控制在 1～3 个核心类型或一个明确算法，避免一次生成太多难以消化的代码。

## 13. 决策记录

| 日期 | 决定 | 原因 | 状态 |
|---|---|---|---|
| 2026-08-24 | 首个游戏选择百家乐 Punto Banco | 规则明确、单手路径有限，适合建立通用概率/EV 内核 | 暂定 |
| 2026-08-24 | 使用 Rust | 用户希望学习 Rust；类型系统和性能适合精确枚举引擎 | 已定 |
| 2026-08-24 | 先做核心库和 CLI | 先验证数学正确性，降低 UI 对调试的干扰 | 暂定 |
| 2026-08-24 | 底层保留牌面与花色 | 后续对子与 Perfect Pair 等边注需要 | 暂定 |
| 2026-08-24 | 主注优先精确枚举 | 单局最多 6 张且可按点数聚合，适合获得可信结果 | 暂定 |
| 2026-08-24 | v0.1 默认使用 8 副牌 | 与项目规则文档一致 | 已定 |
| 2026-08-24 | 标准庄 `0.95:1`、闲 `1:1`、和 `8:1` | 与项目规则文档一致；和局时庄闲 Push | 已定 |
| 2026-08-24 | 免佣庄作为独立规则变体 | 该变体庄 6 点获胜仅赔 `0.5:1`，不能套用标准庄 EV | 已定 |
| 2026-08-24 | 第一批边注为庄对和闲对，均为 `11:1` | 用户指定，规则文档已提供判定和赔率 | 已定 |
| 2026-08-24 | 用户交互使用中文 | 用户指定 | 已定 |
| 2026-08-24 | v0.1 只扣除明确录入的已知牌 | 当前规则未定义不可见烧牌的观测模型 | 已定 |
| 2026-08-24 | 牌靴使用扁平 `[u8; 52]` 计数 | 52 字节计数可直接索引且适合 CPU 缓存；比 bit packing 更简单，支持 8 副牌和 Perfect Pair | 已定 |
| 2026-08-24 | 批量扣牌先统计验证、后统一提交 | 不克隆牌靴，同时保证失败时状态完全不变 | 已定 |
| 2026-08-31 | 二十一点采用美式暗牌 + Peek、默认 8 副牌 3:2 S17 | 与用户确认的首版桌规一致，并保留 H17/6:5 开关 | 已定 |
| 2026-08-31 | 二十一点动作 EV 使用未知暗牌后验占牌模型 | 既不能偷看暗牌，也不能把暗牌错误当成可抽牌 | 已定 |
| 2026-08-31 | 首版 Split 明确标注独立手估算 | 多手联合耗牌和再次分牌递归复杂，不能把近似结果冒充精确值 | 临时 |

## 14. 风险清单

| 风险 | 影响 | 应对 |
|---|---|---|
| 不同赌场规则/赔付表不同 | 同名边注算出错误 EV | 规则配置化，保存规则来源和版本 |
| 把未知烧牌当作已知牌处理 | 条件概率系统性错误 | 单独建模“未知已移除牌” |
| 只记录百家乐点数 | 无法正确扩展对子/花色边注 | 底层使用 13×4 计数 |
| 浮点累计误差 | 概率和偏离、回归测试脆弱 | 路径权重/有理数，展示层再转小数 |
| 补牌表实现错误 | 所有概率和 EV 都错误 | 穷尽规则表测试 + 独立算法对照 |
| 边注太早加入 | 核心模型复杂、难以定位错误 | 主注通过验证后逐项加入 |
| 过早并行或缓存 | 增加复杂性且可能没有收益 | 以 benchmark 和 profiler 为依据 |
| 录牌错误 | 后续整靴结果失真 | undo、历史记录、状态校验与摘要 |
| 幸运 7 局数限制存在冲突 | 将来可能在错误局数开放下注 | 实现该边注前核对：规则第 8～9 节为“前 50 局”，第 12 节为“第 31 局起禁投超级幸运 7” |

## 15. v0.1.0 验收标准

v0.1.0 需同时满足：

- [ ] 支持创建标准 1～8 副牌百家乐牌靴；
- [ ] 能按具体牌面录入、撤销已出现的牌；
- [ ] 正确实现 Natural、闲补牌、庄补牌规则；
- [ ] 精确计算下一把庄、闲、和概率；
- [ ] 根据可配置佣金和赔率计算主注 EV；
- [ ] 支持标准庄和免佣庄两种独立赔付规则；
- [ ] 精确计算庄对、闲对的概率和 `11:1` 赔付下的 EV；
- [ ] CLI 帮助、分析结果和错误信息使用中文；
- [ ] 概率和、牌数和回溯不变量全部通过；
- [ ] 完整牌靴结果通过独立基准验证；
- [ ] CLI 可保存与恢复 session；
- [ ] `cargo fmt`、`cargo clippy`、`cargo test` 全部通过；
- [ ] README 清楚说明规则口径、使用方法和限制。

## 16. 下一次实施建议

Phase 1、Phase 2 和 Phase 3 已完成：牌靴模型、百家乐规则内核、点数聚合概率枚举器、
庄/闲/和权重汇总、完整牌靴基准回归和性能基线均已建立。

Phase 4 已完成，下一步进入 Phase 5，建议仍按小切片推进：

1. 设计中文 CLI 的命令和参数；
2. 实现 `new`、`remove`、`undo`、`shoe` 命令；
3. 实现 `analyze`，输出概率、EV、House Edge 和 RTP；
4. 加入 JSON/TOML 规则配置；
5. 增加 session 保存与恢复。
